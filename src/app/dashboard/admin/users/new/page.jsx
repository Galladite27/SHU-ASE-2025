import ClerkUserManager from "./client";

export default function NewUserPage() {
    return (
        <main className="p-6 sm:p-8 bg-gray-100 min-h-screen">
            <ClerkUserManager />
        </main>
    );
}